%adaugare(L:Lista, R:Lista)
%Modele de flux:(i,o),(i,i)
%L-lista initiala
%R-lista finala in care s-au adaugat elemente
adaugare([],[]).
adaugare([H|T],[H|Rez]):-
    H%2=:=0,
    adaugare([1|T],Rez).

adaugare([H|T],[H|Rez]):-
    H%2=:=1,
    adaugare(T,Rez).

